default:
  image: golang:1.25-alpine

variables:
  CGO_ENABLED: "0"
  GOFLAGS: "-mod=readonly"
  GOLANGCI_LINT_VERSION: "v2.11.3"

stages:
  - lint
  - test
  - build

# Cache Go module downloads across jobs
.go-cache: &go-cache
  cache:
    key:
      files:
        - go.sum
    paths:
      - .go-cache/
  before_script:
    - mkdir -p .go-cache
    - export GOPATH="$CI_PROJECT_DIR/.go-cache"

lint:
  stage: lint
  # Use the official golangci-lint image to avoid wget | sh supply-chain risk
  image: golangci/golangci-lint:${GOLANGCI_LINT_VERSION}-alpine
  script:
    - golangci-lint run ./...

test:
  stage: test
  <<: *go-cache
  script:
    - go test -race -coverprofile=coverage.out ./...
    - go tool cover -func=coverage.out
  coverage: '/total:\s+\(statements\)\s+(\d+\.\d+)%/'
  artifacts:
    paths:
      - coverage.out
    expire_in: 1 week

build:
  stage: build
  <<: *go-cache
  needs: [lint, test]
  script:
    - go build -o bin/service ./cmd/service
  artifacts:
    paths:
      - bin/service
    expire_in: 1 day
